import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Crypto Exchange Latency Monitor",
  description:
    "Real-time 3D visualization of cryptocurrency exchange infrastructure and latency monitoring across AWS, GCP, and Azure",
  keywords: "cryptocurrency, exchange, latency, monitoring, AWS, GCP, Azure, trading, infrastructure",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
